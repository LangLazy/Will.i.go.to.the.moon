#ifndef SIMULATION
#define SIMULATION

#include <vector>
#include <string>
#include "company.h"

void simulation(std::vector<std::vector<Company>> stocks, std::vector<int> normalized);

#endif